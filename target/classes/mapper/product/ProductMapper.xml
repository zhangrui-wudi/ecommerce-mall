<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yourname.mall.module.product.mapper.ProductMapper">

    <select id="selectByCategoryId" resultType="com.yourname.mall.module.product.entity.Product">
        SELECT * FROM products
        WHERE category_id = #{categoryId}
          AND status = 1
          AND is_deleted = 0
        ORDER BY created_at DESC
    </select>

    <select id="searchProducts" resultType="com.yourname.mall.module.product.entity.Product">
        SELECT * FROM products
        WHERE (name LIKE CONCAT('%', #{keyword}, '%') OR description LIKE CONCAT('%', #{keyword}, '%'))
          AND status = 1
          AND is_deleted = 0
        ORDER BY
            CASE
                WHEN name LIKE CONCAT(#{keyword}, '%') THEN 1
                ELSE 2
                END,
            sales_count DESC
    </select>

</mapper>